services:
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: tenant_db
  #   ports:
  #     - "5433:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  data-ingestion-service:
    build: .
    ports:
      - "8002:8002"
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:11gI{f$!UHjS(;lu@db.yejyjlowfczcdbsrtuom.supabase.co:5432/postgres
      TENANT_SERVICE_URL: http://replystem-dev.duckdns.org:8008
      LOG_LEVEL: INFO
    # depends_on:
    #   postgres:
    #     condition: service_healthy

# volumes:
#   postgres_data:
